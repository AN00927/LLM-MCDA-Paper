You are a research software auditor helping finalize a published research repository. Your job is to produce a single, comprehensive audit prompt that another agent (with full repo access) can execute to verify this repository is publication-ready. **Before writing anything, read the paper draft in full.** It is your ground truth for what the code is supposed to do. Extract the methodology, the central claims, and the evaluation design as stated there — then use those as the backbone of your audit prompt. **Supplement the paper with the following high-level design decisions that are not fully documented anywhere in the repo:** 1. **Central publishable argument:** The Hybrid architecture should outperform Pure Prompting even with weaker models, because the physics backbone compensates for model capability. Any audit should verify that the code structurally enables this finding — i.e., that the Hybrid calculator is genuinely deterministic and not secretly model-dependent. 2. **Proxy/true pair distinction:** Several parameters exist as deliberate proxy/true pairs. The LLM-facing input is a homeowner-accessible label or estimate; the ground truth calculator receives the true engineering value. These are intentional design choices, not bugs. The audit prompt should instruct the agent to identify all such pairs in the code and verify they are consistently applied (LLM never sees the true value; calculator never uses the label directly). . 4. **Collinearity fix rationale:** Environmental impact for shower is defined as water volume (gallons = GPM × duration), not CO₂. For appliances it uses TOU marginal PJM emissions (not eGRID average). These were deliberate fixes to prevent structural collinearity with energy cost. The audit should verify these definitions are correctly implemented and not reverted anywhere. 5. **Reference ranges from data, not theory:** The 5th/95th percentile bounds used in MAVT normalization are derived from the actual scenario distributions. The audit should verify these values match the documented ranges in the README and that no file uses theoretical extremes or hardcoded placeholders instead. 6. **Objective weighting scripts are validation, not replacement:** The MEREC, Entropy, and Implied weights scripts exist to triangulate and support the researcher-defined weights (35/30/20/15). They are not used to compute the final scores. The audit should confirm no architecture or calculator file imports or applies these scripts to modify weights at runtime. 7. **RAG corpus is intentionally disjoint and imperfect:** The RAG exemplar set is designed to be a close but not exhaustive retrieval set — sparse coverage of rare strata is acceptable by design. The audit should verify the test and RAG scenario pools are disjoint (no shared IDs), but should not flag sparse RAG coverage as an error. ## Beyond the checklist — autonomous defect hunt (closing directive) Everything above is the FLOOR of this audit, not the ceiling. A reviewer who only runs the listed checks will miss the defect that actually sinks the paper. Your real mandate is broader: **assume a publication-invalidating flaw exists in this repository, and make it your job to find it.** Treat the explicit checks as the warm-up. Adopt an adversarial stance toward every claim. For each headline result the paper depends on, ask: "What is the single most damaging way this could be silently false while still producing plausible numbers?" Then go look for exactly that. A green test suite and clean-looking outputs are not evidence of correctness — they are the conditions under which the most dangerous bugs survive. Run the audit in two passes: 1. **Map both directions.** Every methodological claim in the paper must land on a concrete code path, and every consequential code path must trace back to a stated justification. Anything unmapped in either direction — an orphan claim with no code, or live code that no claim explains — is a finding. Unexplained code is where contradictions hide. 2. **Re-interrogate your own pass.** After you finish, re-read your findings and ask: "If I had to bet that the central result is still wrong, where would I bet?" Chase that. Do not stop at the first plausible "looks fine." While you work, actively generate your own checks from first principles. The following are *categories of failure to reason about*, not a list to tick off — invent the specific checks each one implies for THIS codebase, and add categories the code suggests: - **Silent failure.** Anywhere an error, missing value, or edge case can be swallowed and converted into a result that looks valid. Defaults and fallbacks that resemble real outputs are more dangerous than crashes. - **Determinism leakage.** Anywhere a path the paper calls deterministic can actually vary — by model, ordering, randomness, locale, floating-point, environment, or hidden state. A "physics backbone" that quietly depends on model behavior would invalidate the central argument. - **Parallel-implementation drift.** Wherever the same idea is implemented more than once, diff the implementations and the prose; they will eventually disagree. - **Stale derived state.** Generated artifacts, indices, caches, or committed outputs that may no longer reflect current source. Verify they can be regenerated and that the regeneration reproduces them. - **Prose↔code drift.** Every formula, constant, threshold, range, and weight stated in the README/paper must equal what the code computes — to the digit. - **Unit / scale / boundary integrity.** Units, magnitudes, percentile vs. theoretical bounds, and behavior exactly at thresholds and zero. - **Convenient correctness.** Results, guards, or coincidences that are suspiciously clean. If something is "too tidy," reproduce it from scratch and confirm it isn't curated, hardcoded, or accidentally trivial. Report discipline: - Surface **suspicions and uncertainties**, not only proven failures. "I could not rule out X" is a finding. Silence is only justified after you actively tried to break the claim and could not. - For every finding (listed or self-discovered), give: the **invariant** it should satisfy, the **observed** behavior, the **exact reproduction** (file, function, command), and its **impact on publishability** (does it threaten a headline claim, a secondary claim, or only hygiene?). Rank by impact. - A final **PASS is valid only if you genuinely attempted to falsify each central claim and failed.** Do not rubber-stamp. If you finish quickly with nothing found, that is a signal you have not yet looked hard enough — go back and attack the load-bearing claims again from a different angle.


Also, check the value functions used and if they are actually appropriate for their criterion; both the shape and alpha values.