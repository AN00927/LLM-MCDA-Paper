# Handoff — 2026-08-03

Supersedes the earlier version of this file, whose 6-item task queue is
complete. That queue's results landed in commit `2849c56b` ("feat: update
analysis data and add new draft sections file"). A separate, deliberate
rename pass followed in `56fa79b9` ("renamed files throughout repo"),
renaming roughly 25 scripts to snake_case verb-first names and updating their
in-file references. This file was written after a post-rename integrity
check and documentation sync pass on top of both.

---

## 1. What's finished

**The A_H alternative-ordering experiment is done at full power.** 5 reversed
runs + 3 contemporaneous control runs per model, all four models. Findings:
0/4 models show decision-level (top-1) order sensitivity; 2/4 (Gemini, Qwen)
show parameter-level order sensitivity, surviving Holm correction across the
8-test family (4 models x 2 instruments: parameter-level and choice-level
separation). The two significant p-values sit at the exact permutation floor
of $1/\binom{13}{5} = 1/1287 \approx 0.000777$ — reported as a bound (the true
p is bounded above by that figure), not a point estimate. Outputs live at
`Output Files <MODEL>/hybrid_order_reversal/AH_extraction_order_{control,reversed}_run_NN.xlsx`
plus matching `_raw.jsonl` files. Full writeup: `docs/EXPERIMENTS.md` §2,
`paper/paper_draft_working.tex` `\subsection{Alternative ordering}`
(Methods, \label{sec:alternative-order}) and `\subsection{Alternative
Ordering}` (Results, \label{sec:res-order}).

**The splice into the main draft is done.** `paper/draft_sections_new_analyses.tex`
is now an archive-only provenance record (its own header says so; do not
edit it, do not treat it as a pending task). Its content is in
`paper/paper_draft_working.tex`. That includes the per-model sensitivity
table (`tab:sensitivity_by_model`, \label in §"Sensitivity Analysis"), the
weight-comparison table with the four degenerate Implied rows replaced by a
dedicated zero-variance table (`tab:weight_comparison` and `tab:zero_variance`
in Appendix "Weight Robustness"), and the corrected Discussion prose (entropy
vs. MEREC on Environmental weight, constrained-least-squares description of
implied weights, 285-scenario corpus-scope justification, "All three
differences" fixed from "Both differences").

**The outstanding paper corrections listed in the prior handoff are all
applied.** Verified directly in `paper_draft_working.tex`: the Discussion no
longer claims MEREC assigns above-average weight to Environmental (it now
correctly says MEREC gives it 0.131, the lowest of its four, versus entropy's
0.371); implied weights are described as constrained least squares, not
logistic regression; the 285-scenario corpus scope is explicitly justified by
value-function calibration; the ±0.05 single-criterion perturbation rows are
absent from both the main sensitivity table and the supplement's
`tab:sensitivity_top1`.

**The rename pass is functionally complete, with four bugs found and fixed
this session** (see §2). `EntropyWeights.py` was deliberately left unrenamed
(confirmed against the filesystem, not just the rename map).

---

## 2. What this session did (post-rename integrity check + doc sync)

Static analysis only — no architecture, benchmark, ablation, or pipeline
script was executed; nothing here spent an API call.

**Four functional import bugs fixed** (each was a script continuing to
import a pre-rename module name that no longer resolves):
- `paper_pipeline/generate_variance_plot_tex.py` imported `generate_violin_tex`
  (renamed to `generate_violin_plot_tex`).
- `Miscellaneous Scripts/SensitivityAnalysis.py` imported `CalculateMetrics`
  (renamed to `evaluate_architecture_metrics`).
- `Miscellaneous Scripts/significance_testing.py` imported `CalculateMetrics`
  (same rename).
- `Miscellaneous Scripts/run_baseline_models.py` imported `RunRAGAblations`
  in two places (renamed to `run_rag_ablation_experiments`).

All four are load-bearing: `generate_variance_plot_tex.py` is one of the two
steps `paper_pipeline/run_paper_pipeline.py` actually runs, and the other
three are standalone scripts that would have failed on their first line of
real work the next time someone invoked them.

**Comment/docstring-only stale filename references fixed** (non-functional,
but confusing): `test_prompt_ablation_significance.py`,
`AggregatePromptAblations.py`, `CreateRepresentativeSample.py` (3 mentions),
`run_prompt_ablation_experiments.py`, `run_hybrid_ablation_experiments.py`,
`WeightDiagnostics.py`, and two mentions in `XLSX_Schema_Map.md`.

**`docs/metrics_calculation_pipeline.md` script-name refresh:** fixed
`Significance_testing.py` → `significance_testing.py` (casing) in four
places, `CalculateMetrics` → `evaluate_architecture_metrics.py`, and the §9
heading `numbers_master.py` → `generate_paper_results_numbers.py`. No leftover
task-ID marker (e.g. a stray `C5:`) was found in this file.

**Not fixed — reported instead:** `sentinel_utils.py` carries five
docstring-only mentions of the pre-rename names `BuildRAG` and
`MERCECWeights`/`ImpliedWeights`. This is a widely-imported core file; five
mentions is past the "isolated single token" bar this session used as its
edit threshold, so it was left alone rather than mass-edited. Someone should
sweep it deliberately.

**Everything else checked and found already correct:** every `spec_from_file_location`
call in `Architectures/`, `Miscellaneous Scripts/`, `paper_pipeline/`, and
`tests/` resolves to a real file; `run_paper_pipeline.py`'s two `subprocess`
calls both resolve; `docs/EXPERIMENTS.md`, `docs/CODEBASE_GUIDE.md`, and
`docs/PROVENANCE_AUDIT_PROMPT.md` had no leftover pre-rename names;
`docs/EXPERIMENTS.md` already correctly describes the 5-run/3-run arms, the
8-test Holm family, and the permutation floor as a bound. `python -m
compileall` and targeted `py_compile` runs found no syntax damage anywhere
touched by the rename.

**Two genuine bugs found and reported, not fixed, because they need a
judgment call:**

1. **Pooled-across-models headline numbers survive in the paper.** The
   abstract states "$\mathcal{A}_{\text{H}}$ achieved a Kendall's tau of
   0.899 ... $\mathcal{A}_{\text{E}}$ scored 0.277 ... $\mathcal{A}_{\text{D}}$
   0.093," and the same three figures (plus matching Top-1 percentages
   34.1/49.2/91.3) recur in the Overall Ranking Accuracy narrative, Failure
   Analysis, and Limitations. Each figure is the arithmetic mean of that
   architecture's four per-model figures (verified: e.g. 0.093 =
   mean(0.176, 0.144, 0.041, 0.010) for $\mathcal{A}_{\text{D}}$'s
   per-model $\tau$ in Figure~\ref{fig:grouped_bars}). This is a genuine
   four-model average used as a headline statistic, which is exactly what
   the project's no-pooling rule prohibits — and the rule exists because a
   pooled mean previously hid an $\mathcal{A}_{\text{D}}$/$\mathcal{A}_{\text{E}}$
   reversal (see the sensitivity-analysis discussion, §4.7/App. B, where
   $\mathcal{A}_{\text{D}}$ beats $\mathcal{A}_{\text{E}}$ in some
   model x weight-vector cells). This is long-standing — it predates this
   session and is not a rename artifact — so it was not touched. Removing it
   from the abstract and four other passages requires deciding how to
   restate a one-sentence headline as four numbers, which is an authorial
   call, not a mechanical fix.
2. **The new A_H order-reversal runs do not record a UTC collection
   timestamp**, despite the `CLAUDE.md` convention (under "Conventions that
   MUST hold") added this session requiring one. Checked directly: neither
   the `_raw.jsonl` records nor the per-run `.xlsx` columns for
   `AH_extraction_order_reversed_run_01` (GPT-OSS) contain a timestamp
   field. The convention was written down after these runs were collected,
   so this is not a regression, but it means the gap the convention exists
   to close (a run whose vintage can't be verified independently of git
   history) is still open for this experiment's outputs specifically.

**Two cross-reference fixes in `paper/supplementary_material.tex`:** two
occurrences of "Section~3.8 of the main paper" (describing the two-stage
Holm correction protocol) pointed at `paper_draft_working.tex`'s §3.8
("Alternative ordering"), which does not contain that protocol — it's
defined in §3.7 ("Ablation Designs"). Both fixed to "Section~3.7." (The
supplement compiles standalone and cannot use `\ref` across documents, so
these are necessarily hardcoded and worth spot-checking again if section
numbering in the main draft changes.)

**`paper_pipeline` clobber-risk check (Task 3 of this session's brief):**
`run_paper_pipeline.py` currently runs exactly two steps —
`calculate_per_run_metrics.py --all-models` and `generate_variance_plot_tex.py`
— which write only to `paper/per_run_metrics/` (per-run CSVs/xlsx and
`variance_plots.tex`). `tab:sensitivity_by_model`, `tab:sensitivity_top1`,
`tab:zero_variance`, and `tab:weight_comparison` are literal hand-written
LaTeX tables embedded directly in `paper_draft_working.tex` /
`supplementary_material.tex`, not `\input` from a generated snippet, and
their source scripts (`SensitivityAnalysis.py`, `WeightDiagnostics.py`,
`merec_weights.py`, `implied_weights.py`, `EntropyWeights.py`) are not
invoked anywhere in `run_paper_pipeline.py`. **Verdict: all four are safe
from re-running the master pipeline.** The only way to clobber them is to
manually re-run one of those five standalone scripts and hand-paste fresh
numbers back into the tables — a deliberate action, not an accidental one.

**`paper/paper_draft_working_DOUBLE.tex` confirmed stale**: last touched at
commit `e22cb328`, before both the analysis-and-splice commit and the rename
pass. Not edited, per instruction.

---

## 3. What remains open

**The standing request, unchanged and still the headline next task:** a full
**citation and claim audit** of Results, Discussion, and the 78KB
`paper/supplementary_material.tex`. None of the three has ever been
adversarially reviewed. Check every citation and every claim for
methodological defensibility and reviewer-rejection risk, and judge whether
each problem found is small enough to live in Limitations or big enough to
need a claim change. Use subagents; follow the reading protocol in
`CLAUDE.md` (dispatch an explore agent to map section structure first, by
`\label`, not line number — line numbers drift).

**Secondary, smaller items surfaced this session:**
- Decide what to do about the pooled-across-models headline numbers in the
  abstract and four other passages (§2 above). This needs an authorial
  decision, not a mechanical edit.
- Decide whether the A_H order-reversal runs need a retroactive timestamp
  note, or whether the `CLAUDE.md` convention's forward-only scope is
  acceptable as-is.
- Sweep `sentinel_utils.py`'s five stale `BuildRAG`/`MERCECWeights`/
  `ImpliedWeights` docstring mentions deliberately (not done this session;
  see §2).

### Rules

- **Never `git commit` or `git push`.** The user handles all commits. This
  is absolute.
- Unexpected git state is usually the user working concurrently. Note it
  once, do not investigate.
- Do not edit Introduction, Literature Review, or Methodology up to and
  including §2.2 (MAVT framework design) without asking.
- Paper edits: change the minimum. Word-level swaps over sentence rewrites;
  do not reword for clarity, reorder, or restructure.
- Never pool metrics across the four models. Flag a pooled number; do not
  invent a per-model replacement without checking with the user first.
- Ask questions during the work, not batched at the end.
